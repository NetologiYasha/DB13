SELECT title, MAX(trak_duration) FROM trak

SELECT title, trak_duration FROM trak 
WHERE trak_duration >00:03:29;

SELECT collection, year_of_release = 2018 and 2020

SELECT title, From trak 
WHERE title Not Like '% %'

SELECT title = "мой" or "my"